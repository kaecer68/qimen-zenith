# 此專案的 runtime ports / URLs 由 scripts/sync-contracts.sh 生成到 .env.ports。
# 請勿手動維護 .env.ports，也不要在其他啟動腳本中硬寫 port fallback。
#
# 常見覆寫只保留給非 port 類設定；若需要本機覆寫 host/URL，請明確 export。
# QIMEN_GRPC_HOST=127.0.0.1:50055
# LUNAR_API_URL=http://127.0.0.1:8080
